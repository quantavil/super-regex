'use strict';

var obsidian = require('obsidian');

// ... (keep all the existing helper functions and classes until the main plugin class)

// Add this new View class


// Modified Main Plugin Class


// Keep the existing FindAndReplaceModal class for backwards compatibility if needed
// ... (keep the existing modal code but don't use it in commands)

// Keep the existing RegexFindReplaceSettingTab class unchanged
// ... (keep all the settings tab code as is)

module.exports = RegexFindReplacePlugin;